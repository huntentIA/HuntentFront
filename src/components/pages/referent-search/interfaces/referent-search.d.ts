interface SearchInfoModalProps {
  isOpen: boolean
  onClose: () => void
}

interface TooltipProps {
  children: React.ReactNode
  content: string
}

interface ReferentSearchProps {
  isDarkMode: boolean
}

interface UserData {
  id: string
}
